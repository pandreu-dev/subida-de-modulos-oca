<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sustituto en codigo de la personalizacion de Studio
         "res.partner.form customization": desactiva el SMS en el campo Telefono. -->
    <record id="view_partner_form_no_sms" model="ir.ui.view">
        <field name="name">res.partner.form.no.sms</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="attributes">
                <attribute name="options">{"enable_sms": false}</attribute>
            </xpath>
        </field>
    </record>
</odoo>
